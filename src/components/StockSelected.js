import React from 'react';

const StockSelected = ({selectedStock, fetchSelectedStock}) => {

  return (
  <div id="stock-selected">
    selected stock component
  </div>
  )
}

export default StockSelected;
